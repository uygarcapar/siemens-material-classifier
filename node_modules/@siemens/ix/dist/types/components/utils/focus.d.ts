export declare class ArrowFocusController {
    items: Element[];
    container?: HTMLElement;
    wrap: boolean;
    callback: any;
    private readonly keyListener;
    constructor(items: any[], container: HTMLElement, callback?: (index: number) => void);
    private getActiveIndex;
    private onKeyDown;
    disconnect(): void;
}
