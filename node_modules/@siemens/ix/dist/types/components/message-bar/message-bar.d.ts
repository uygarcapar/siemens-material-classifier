import { EventEmitter } from '../../stencil-public-runtime';
import { NotificationColor } from '../utils/notification-color';
export declare class MessageBar {
    /**
     * Specifies the type of the alert.
     * @deprecated Type `danger` will be removed in 4.0. Use `alarm` instead.
     */
    type: 'alarm' | 'danger' | 'critical' | 'warning' | 'success' | 'info' | 'neutral' | 'primary';
    /**
     * If true, close button is enabled and alert can be dismissed by the user
     */
    dismissible: boolean;
    /**
     * An event emitted when the close button is clicked
     */
    closedChange: EventEmitter;
    /**
     * An event emitted when the close animation is completed
     */
    closeAnimationCompleted: EventEmitter;
    icon?: string;
    color?: NotificationColor;
    private static readonly duration;
    private static readonly messageTypeConfigs;
    private divElement?;
    componentWillRender(): void;
    private closeAlert;
    render(): any;
}
